# Screenshots

Add real screenshots of the live app here, then reference them from the
main `README.md`. Suggested set:

- `home.png` — landing page
- `booking.png` — cylinder booking/checkout flow
- `orders.png` — customer order tracking
- `admin-orders.png` — admin orders dashboard
- `retailer-dashboard.png` — retailer accept/reject view
- `dark-mode.png` — dark mode
- `mobile.png` — mobile view

Keep images reasonably sized (under ~500KB each) so the repo doesn't
bloat — a PNG straight from a screenshot tool is usually fine, but run
it through something like [TinyPNG](https://tinypng.com) first if it's
large.
